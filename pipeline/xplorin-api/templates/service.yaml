apiVersion: v1
kind: Service
metadata:
  name: {{ include "xplorin-api.fullname" . }}
  labels:
    {{- include "xplorin-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xplorin-api.selectorLabels" . | nindent 4 }}
